@model IEnumerable<pureba2register.Models.prueba>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.campoPruena)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.campoPruena)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pruebaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.pruebaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pruebaID })
        </td>
    </tr>
}

</table>
